import tkinter as tk
class App():
    def __init__(self):
        self.root = tk.Tk()
        self.root.title("实验2")
        self.Spinbox = Spinbox(root, from_=0, to=10)
        self1.Button = tk.Button(text="分配空间", command = )
        self1.Button.pack()
        self2.Button = tk.Button(text="回收空间", command = )
        self2.Button.pack()

        self.root.mainloop()